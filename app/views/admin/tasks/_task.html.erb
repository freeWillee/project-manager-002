
<tr>
  <td><%= link_to task.title, admin_task_path(task) %></td>
  <% if !params[:user_id] %>
    <td><%= task.user.username %> (<a href="
      mailto: <%= task.user.email %>">email</a>)</td>
  <% end %>
  <td><%= task.percent_complete %></td>
  <td><%= link_to "Edit", edit_admin_task_path(task) %></td>
  <td><%= link_to task.project.name, admin_project_path(task.project) %></td>
</tr>
