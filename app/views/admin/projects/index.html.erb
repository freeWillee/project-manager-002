<h1>All Projects</h1>

<% if @projects.empty? %>
  <p>There are no projects to show</p>
<% else %>
  <table>
    <thead>
      <tr>
        <th>Project Name</th>
        <th>Due Date</th>
        <th>Details</th>
        <th>Action</th>
      </tr>
    </thead>
    <tbody>
      <p><%= render(@projects) %></p>
    </tbody>
  </table>
<% end %>
