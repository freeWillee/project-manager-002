<div class="center">
  <h1>All Projects</h1>
  <section class="info">
    <div class="container">
      <ul>
        <% @projects.each do |project| %>
          <li>Name: <%= project.name %></li>
        <% end %>
      </ul>
    </div>
  </section>
</div>
